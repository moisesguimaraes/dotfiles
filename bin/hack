#!/bin/bash -x
# hack: Rebase the latest changes from the master branch into your working branch
ref=$(git symbolic-ref HEAD &2> /dev/null) || exit 0
CURRENT="${ref#refs/heads/}"
git checkout master
git pull upstream master
git checkout ${CURRENT}
git rebase master